<%# <div class="container mt-3 mb-7"> %>

  <%# <div class="row"> %>

    <%# <div class="col-6"> %>
      <%# <div> %>
        <%# <%= cl_image_tag @vehicle.photos.first.key %> %>
      <%# </div> %>
    <%# </div> %>

    <%# <div class="col-6">
      <div class="cards">
      <% @vehicle.photos.each do |photo| %>
      <%# <%= cl_image_tag photo.key, height: 100, width: 100, crop: :fill %>
      <%# <% end %>
      <%# </div> %>
    <%# </div> %>

  <%# </div>
</div> %>

<div class="parent">
<% @vehicle.photos.each do |photo| %>
      <%# <%= cl_image_tag photo.key, height: 100, width: 100, crop: :fill %>
      <%# <% end %>
<div class="div1"><%= cl_image_tag photo.key, height: 300, width: 350, crop: :fill %> </div>
<div class="div2"><%= cl_image_tag photo.key, height: 150, width: 300, crop: :fill %> </div>
<div class="div3"><%= cl_image_tag photo.key, height: 150, width: 300, crop: :fill %></div>
<div class="div4"><%= cl_image_tag photo.key, height: 150, width: 300, crop: :fill %></div>
<div class="div5"><%= cl_image_tag photo.key, height: 150, width: 300, crop: :fill %></div>
<% end %>
</div>



<div class="text-start">
  <p><%= @vehicle.description %></p>
</div>

<div class="card-product">
  <img src="" />
  <div class="card-product-infos">
    <h2> <%= @vehicle.name %></h2>
    <%= @vehicle.price_per_day %>
    <p>Year : <%= @vehicle.year %></p>
    <p>Type : <%= @vehicle.category%></p>
    <p><%= @vehicle.available ? "available" : "not available for now" %></p>
  </div>
</div>

<div class="bg-danger text-white">
<div class="text-center">
  <h3>CHOOSE YOUR DATES</h3>
</div>
<div class="d-flex justify-content-evenly">
  <div>
    <label for="start_date">Start Date</label>
    <input type="date" name="start_date" id="start_date">
  </div>

  <div>
    <label for="end_date">End Date</label>
    <input type="date" name="end_date" id="end_date">
  </div>
</div>

  <div class="text-center">
    <button type="button" class="btn btn-success">
      <%= link_to "Rent", booking_path %>
    </button>
  </div>

</div>

<br>
 <button type="button" class="btn btn-danger">
    <%= link_to "Back to home", vehicles_path %>
  </button>
